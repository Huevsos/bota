{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "chmod +x main.py && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "./main.py",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
